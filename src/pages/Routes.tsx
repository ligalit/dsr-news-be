import React from 'react';
import {
    Routes,
    Route,
} from "react-router-dom";

const RoutesComponent = () => {
    return (
        <Routes>
            <Route path="/" element={""}/>
        </Routes>
    );
};

export default RoutesComponent;